'use strict';
angular
.module('nonAdmin')
.controller('Contact', ['$scope', function ($scope) {
  var vm = this;
  vm.isTabActive = function (index) {
  };
}])
.controller('Feedback', ['$scope', function ($scope) {
  var vm = this;
  vm.isTabActive = function (index) {
  };
}]);
